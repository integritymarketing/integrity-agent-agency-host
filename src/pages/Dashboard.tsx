const Dashboard = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Welcome to the dashboard!</p>
    </div>
  );
};

Dashboard.propTypes = {
  // No props needed for this component currently
};

export default Dashboard;
